server:
  port: 8100
spring:
  profiles:
    active: default
  application:
    name: account-service
  cloud:
    config:
      server:
        git:
          uri: http://localhost:8888
          label: latest