server:
  port: 8100
spring:
  profiles:
    active: default
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: default
management:
  endpoints:
    web:
      exposure:
        include: '*'