server:
  port: 8300
spring:
  profiles:
    active: default
  application:
    name: operation-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: default
management:
  endpoints:
    web:
      exposure:
        include: '*'