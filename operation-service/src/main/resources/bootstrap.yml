#Now port is generated randomly. Further communication through gateway only!
server:
  port: 8200

spring:
  datasource:
    url: jdbc:mysql://piotrsarnecki.com:3306/sarpio_operation
    username: sarpio_operation
    password: Operation1!
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: operation-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database-platform: "org.hibernate.dialect.MySQL8Dialect"
  liquibase:
    change-log: 'classpath:liquibase/liquibase-changeLog.xml'
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
management:
  endpoints:
    web:
      exposure:
        include: '*'