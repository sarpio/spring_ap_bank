server:
  port: 8300
spring:
  profiles:
    active: default
  application:
    name: operation-service
  cloud:
    config:
      server:
        git:
          uri: http://localhost:8888
          label: latest