server:
  port: 8200
spring:
  profiles:
    active: default
  application:
    name: customer-service
  cloud:
    config:
      server:
        git:
          uri: http://localhost:8888
          label: latest