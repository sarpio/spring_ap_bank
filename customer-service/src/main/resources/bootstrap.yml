server:
  port: 8200
spring:
  profiles:
    active: default
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: default
management:
  endpoints:
    web:
      exposure:
        include: '*'